---
# Use the 'create api' subcommand to add watches to this file.
- version: v1
  group: storage.csas.cz
  kind: Nfs
  role: nfs
  finalizer:
    name: storage.csas.cz/finalizer
    vars:
      state: absent
#+kubebuilder:scaffold:watch
